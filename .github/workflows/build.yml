name: Build Power BI Visual
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install pbiviz
        run: npm install -g powerbi-visuals-tools
      - name: Install dependencies
        run: cd warehouseVisual && npm install
      - name: Build visual
        run: cd warehouseVisual && node --max-old-space-size=8192 $(which pbiviz) package
      - name: Upload pbiviz file
        uses: actions/upload-artifact@v4
        with:
          name: visual-package
          path: warehouseVisual/dist/*.pbiviz
